{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Ejecutar run.sh (task)",
      "type": "coreclr",          // un tipo válido cualquiera, no importa
      "request": "launch",
      "preLaunchTask": "Run run.sh"
    }
  ]
}