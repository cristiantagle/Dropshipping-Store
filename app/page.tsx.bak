import Hero from "@/components/Hero";
import ProductListClient from "@/components/ProductListClient";
import { getAllProducts } from "@/lib/products";

export default async function HomePage() {
  const products = (await getAllProducts()).slice(0, 8);

  return (
    <main>
      <Hero />
      <section id="nuevos" className="max-w-7xl mx-auto px-4 py-12">
        <h2 className="text-2xl font-bold mb-6">Nuevos</h2>
        <div className="overflow-x-auto">
          <div className="flex gap-6">
            <ProductListClient initialProducts={products} />
          </div>
        </div>
        <div className="mt-6 text-center">
          <a
            href="/categorias"
            className="inline-block bg-lime-600 hover:bg-lime-700 text-white px-6 py-3 rounded-md font-semibold"
          >
            Ver todos
          </a>
        </div>
      </section>
    </main>
  );
}
